<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="searchDao">
 	 <select id="getSearchList" resultType="searchDto">
        SELECT search_term FROM sys.search_history ORDER BY search_date DESC LIMIT 5
    </select>
 	<insert id="search_insert" parameterType="searchDto">
 		insert into search_history (user_id, search_term, search_date) values (#{user_id}, #{search_term}, now())
 	</insert>
 </mapper>
 
